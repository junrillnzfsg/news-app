import SelectData from '../model/SelectData'

const newsCategory: SelectData[] = [
    { key: 'top-headlines', value: 'top-headlines', text: 'Top Headlines' },
    { key: 'everything', value: 'everything', text: 'Everything' },
]

export default newsCategory
